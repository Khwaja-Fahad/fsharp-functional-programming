# Option Type in F#

This project demonstrates the Option type in F#.

Option is used instead of null to represent values that may or may not exist.

Examples included:
- safe division
- pattern matching with Some and None
- handling optional values safely
